# API Aberta — Fuel Prices Connector

PORT=3001
NODE_ENV=production

# MongoDB
MONGO_URI=mongodb://localhost:27017/apiaberta-fuel

# Sync schedule (cron format, default: daily at 6 AM)
# SYNC_SCHEDULE=0 6 * * *
